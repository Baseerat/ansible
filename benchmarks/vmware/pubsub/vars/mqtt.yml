---

pubsub_broker:
  prme_id: 2
  vm_id: 120

pubsub_nodes:
  prme1-k8s-node-120:
    type: "publisher"
    count: 10
    broker: "{{ pubsub_broker }}"
  prme2-k8s-node-120:
    type: "broker"
    broker: "{{ pubsub_broker }}"
  prme3-k8s-node-120:
    type: "subscriber"
    count: 20
    broker: "{{ pubsub_broker }}"
