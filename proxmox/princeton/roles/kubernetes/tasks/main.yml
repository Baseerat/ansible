---

- include: install_common.yml
  tags: common
  when:
    "'masters' in group_names or 'minions' in group_names"

- include: ssl_cert_gen.yml
  tags: ssl_gen
  when:
    "'masters' in group_names"

- include: master_build.yml
  tags: master
  when:
    "'masters' in group_names"

- include: minion_build.yml
  tags: minion
  when:
    "'minions' in group_names"